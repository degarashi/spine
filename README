・What's this
	趣味で自分用に作っているオブジェクトプール、リソースマネージャ等のユーティリティライブラリ

・目的
	自作ゲームで利用する前提で、 ヘッダオンリーで作っていこうかと思う。
	拙作のspinnerのうち、数値計算クラスを分離したものがfrea、残りをこのライブラリが吸収する。
	また、規模の大きなライブラリに依存しない。

<実用性は無いと思います>

依存ライブラリ:
	GoogleTest (単体テストをする場合のみ)
	Cereal (シリアライズ用)
	boost::preprocessor
依存ライブラリ(予定):
	boost::format
